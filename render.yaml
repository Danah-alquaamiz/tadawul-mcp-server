services:
  - type: web
    name: tadawul-mcp-server
    env: python
    region: oregon
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps chromium
    startCommand: python server.py
    envVars:
      PYTHONUNBUFFERED: "1"

